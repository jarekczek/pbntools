RELEASE

- Bump version in resource\jc\pbntools\PbnTools.properties.
- Bump year src\jc\pbntools\DlgPbnToolsAbout.java and build.gradle (copyright)
- Give links to previous version in help (help_common.tex),
  binaries and source.
- Changes list in doc/help_??.tex pl i en, last modified.
- Inspect libraries source code links, may be outdated
- Any new command line options? Add them to doc.
- gradlew clean doc (necessary after version bump, sometimes 2 attempts needed).
  Sometimes en does not run, then gradlew clean doc helps,
  because only the English version is then rebuilt
  and Polish intermediate files may corrupt this process.
  maybe gradlew clean-doc doc
- Compare help html files with previous release (useful to replace "tex2html[0-9]+" with "tex2html" beforehand).
- gradlew junit-www (strange dependencies here, junit-www downloads to work/junit-tmp, which is needed for junit-local)
- gradlew junit-local (so instead of gradlew junit, which includes both, we call 2 separately)
- git commit and push
- verify all is commited
- gradlew release
- Rename release files, remove commit ref.
- Upload to home page (3 release files, 2 help files).
- Test help online:
  mit license
  toc
- Upload dependencies (help up to date?)
- Upload to sourceforge.
- Set default versions at sourceforge (linux for linux, src for bsd, solaris, others).

BUILDING ZBAR

Windows (Visual Studio)
- Download zbarw
- Patch
- Download gnuwin32 iconv (this is possibly outdated, unnecessary step, I think package manager is used)
- Build using Visual Studio 2022

Linux (Debian)
sudo apt-get update
sudo apt-get install mercurial autoconf gettext build-essential autoconf automake libtool pkg-config \
  libmagickwand-dev libgraphicsmagick1-dev tmux libgtk2.0-dev xmlto libxext-dev libjpeg62-turbo-dev -y
hg clone http://hg.code.sf.net/p/zbarw/code zbarw-code
cd zbarw-code
autoreconf --install
CFLAGS="-Wno-logical-not-parentheses" \
  ./configure --without-python --without-qt --without-imagemagick --without-gtk --disable-shared
make
sudo make install
make html-local
make install DESTDIR=~/zbar_installation
cd ~/zbar_installation
tar -czvf ~/zbar_build.tar.gz .

running:
sudo apt-get install libjpeg62-turbo-dev
